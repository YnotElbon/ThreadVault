#!/usr/bin/env bash
set -euo pipefail
# Deprecated wrapper: route to unified consult tool
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -t 2 ]; then
  echo "[ask] Deprecated. Use: system/bin/consult" >&2
fi
exec "$SCRIPT_DIR/consult" "$@"
